import { getLogger } from './logger.js';
const componentLoggers = new Map();
export function getComponentLogger(component) {
    let logger = componentLoggers.get(component);
    if (!logger) {
        logger = getLogger().child({
            component,
        });
        componentLoggers.set(component, logger);
    }
    return logger;
}
export function clearComponentLoggers() {
    componentLoggers.clear();
}
//# sourceMappingURL=component.js.map