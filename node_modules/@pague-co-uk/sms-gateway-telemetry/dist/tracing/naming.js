export const spanNaming = {
    http(request) {
        return `${request.method} ${request.path ?? request.url}`;
    },
    nest(controller, handler) {
        return `${controller}.${handler}`;
    },
    rabbitmq(exchange, routingKey) {
        return `RabbitMQ ${exchange}:${routingKey}`;
    },
    queue(queue) {
        return `Queue ${queue}`;
    },
    smpp(command) {
        return `SMPP ${command}`;
    },
    database(operation, table) {
        return table
            ? `DB ${operation} ${table}`
            : `DB ${operation}`;
    },
    cache(operation, key) {
        return `Cache ${operation}`;
    },
    externalHttp(method, host) {
        return `${method} ${host}`;
    },
    custom(name) {
        return name;
    },
};
//# sourceMappingURL=naming.js.map