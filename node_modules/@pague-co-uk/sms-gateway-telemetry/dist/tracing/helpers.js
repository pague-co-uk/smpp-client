import { startActiveSpan } from './span.js';
export async function withSpan(name, operation) {
    return startActiveSpan(name, async (span) => {
        try {
            return await operation(span);
        }
        catch (error) {
            span.recordException(error);
            throw error;
        }
        finally {
            span.end();
        }
    });
}
export function withSpanSync(name, operation) {
    return startActiveSpan(name, (span) => {
        try {
            return operation(span);
        }
        catch (error) {
            span.recordException(error);
            throw error;
        }
        finally {
            span.end();
        }
    });
}
//# sourceMappingURL=helpers.js.map