import { context, trace, } from '@opentelemetry/api';
import { Observable } from 'rxjs';
import { failSpan, finishSpan, startSpan, } from './span.js';
function createSpanContext(name, options, parentContext) {
    const span = startSpan(name, options, parentContext);
    return {
        span,
        context: trace.setSpan(context.active(), span),
    };
}
export function withSyncSpan(name, callback, options, parentContext) {
    const { span, context: spanContext, } = createSpanContext(name, options, parentContext);
    return context.with(spanContext, () => {
        try {
            const result = callback(span);
            finishSpan(span);
            return result;
        }
        catch (error) {
            failSpan(span, error);
            throw error;
        }
    });
}
export async function withPromiseSpan(name, callback, options, parentContext) {
    const { span, context: spanContext, } = createSpanContext(name, options, parentContext);
    return context.with(spanContext, async () => {
        try {
            const result = await callback(span);
            finishSpan(span);
            return result;
        }
        catch (error) {
            failSpan(span, error);
            throw error;
        }
    });
}
export function withObservableSpan(name, callback, options, parentContext) {
    return new Observable((subscriber) => {
        const { span, context: spanContext, } = createSpanContext(name, options, parentContext);
        const source = context.with(spanContext, () => callback(span));
        const subscription = context.with(spanContext, () => source.subscribe({
            next(value) {
                subscriber.next(value);
            },
            error(error) {
                failSpan(span, error);
                subscriber.error(error);
            },
            complete() {
                finishSpan(span);
                subscriber.complete();
            },
        }));
        return () => {
            if (!subscription.closed) {
                subscription.unsubscribe();
            }
            if (span.isRecording()) {
                finishSpan(span);
            }
        };
    });
}
//# sourceMappingURL=execution.js.map