import type { HttpRequest } from '../http/index.js';
export declare const spanNaming: {
    http(request: HttpRequest): string;
    nest(controller: string, handler: string): string;
    rabbitmq(exchange: string, routingKey: string): string;
    queue(queue: string): string;
    smpp(command: string): string;
    database(operation: string, table?: string): string;
    cache(operation: string, key?: string): string;
    externalHttp(method: string, host: string): string;
    custom(name: string): string;
};
//# sourceMappingURL=naming.d.ts.map