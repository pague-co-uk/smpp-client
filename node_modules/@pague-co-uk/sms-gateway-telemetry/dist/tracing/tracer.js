import { trace } from '@opentelemetry/api';
import { NotInitializedError } from '../common/errors.js';
let tracer;
export function initTracer(config) {
    if (tracer) {
        return tracer;
    }
    tracer = trace.getTracer(config.serviceName, config.version);
    return tracer;
}
export function getTracer() {
    if (!tracer) {
        throw new NotInitializedError('Tracer');
    }
    return tracer;
}
//# sourceMappingURL=tracer.js.map