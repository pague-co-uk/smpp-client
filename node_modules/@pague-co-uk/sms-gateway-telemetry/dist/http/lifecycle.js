import { performance } from 'node:perf_hooks';
import { context, trace } from '@opentelemetry/api';
import { establishRequestContext, } from './context.js';
import { logRequestCompleted, logRequestFailed, logRequestStarted, } from './logger.js';
import { getHttpMetrics } from './metrics.js';
import { finishHttpSpan, failHttpSpan, startHttpSpan, } from './tracing.js';
function routeFor(request) {
    if (request.route?.path) {
        return `${request.baseUrl ?? ''}${request.route.path}`;
    }
    return request.path ?? request.url.split('?')[0] ?? request.url;
}
export class HttpRequestLifecycle {
    request;
    response;
    startTime;
    span;
    metrics = getHttpMetrics();
    completed = false;
    constructor(request, response, contextOptions) {
        this.request = request;
        this.response = response;
        establishRequestContext(request, response, contextOptions);
        this.span = startHttpSpan(request);
        this.startTime = performance.now();
    }
    start() {
        this.metrics.requestStarted({
            method: this.request.method,
            route: routeFor(this.request),
        });
        logRequestStarted(this.request);
    }
    finish() {
        if (this.completed) {
            return;
        }
        this.completed = true;
        const duration = performance.now() - this.startTime;
        this.metrics.requestCompleted(duration, {
            method: this.request.method,
            route: routeFor(this.request),
            status: this.response.statusCode,
        });
        logRequestCompleted(this.request, this.response.statusCode, duration);
        finishHttpSpan(this.span, this.response.statusCode);
    }
    fail(error) {
        if (this.completed) {
            return;
        }
        this.completed = true;
        const duration = performance.now() - this.startTime;
        this.metrics.requestCompleted(duration, {
            method: this.request.method,
            route: routeFor(this.request),
            status: this.response.statusCode || 500,
        });
        logRequestFailed(this.request, error, this.response.statusCode || 500, duration);
        failHttpSpan(this.span, error, this.response.statusCode || 500);
    }
    run(callback) {
        return context.with(trace.setSpan(context.active(), this.span), callback);
    }
}
export function createHttpRequestLifecycle(request, response, contextOptions) {
    return new HttpRequestLifecycle(request, response, contextOptions);
}
//# sourceMappingURL=lifecycle.js.map