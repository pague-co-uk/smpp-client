import { CounterMetric, createCounterMetric, createGaugeMetric, createHistogramMetric, GaugeMetric, HistogramMetric, } from '../metrics/index.js';
import { createLazyMetricBundle } from '../metrics/lazy.js';
import { DEFAULT_ACTIVE_REQUESTS, DEFAULT_REQUEST_COUNTER, DEFAULT_REQUEST_DURATION, DEFAULT_REQUEST_SIZE, DEFAULT_RESPONSE_SIZE, } from './constants.js';
class HttpMetrics {
    requests;
    requestDuration;
    requestSize;
    responseSize;
    activeRequests;
    activeRequestCount = 0;
    constructor() {
        this.requests = createCounterMetric({
            name: DEFAULT_REQUEST_COUNTER,
            description: 'Total HTTP requests',
        });
        this.requestDuration =
            createHistogramMetric({
                name: DEFAULT_REQUEST_DURATION,
                description: 'HTTP request duration',
                unit: 'ms',
            });
        this.requestSize =
            createHistogramMetric({
                name: DEFAULT_REQUEST_SIZE,
                description: 'HTTP request size',
                unit: 'By',
            });
        this.responseSize =
            createHistogramMetric({
                name: DEFAULT_RESPONSE_SIZE,
                description: 'HTTP response size',
                unit: 'By',
            });
        this.activeRequests =
            createGaugeMetric({
                name: DEFAULT_ACTIVE_REQUESTS,
                description: 'Active HTTP requests',
            }, () => this.activeRequestCount);
    }
    requestStarted(attributes) {
        this.activeRequestCount++;
    }
    requestCompleted(durationMs, attributes) {
        this.activeRequestCount = Math.max(0, this.activeRequestCount - 1);
        this.requests.increment(attributes);
        this.requestDuration.record(durationMs, attributes);
    }
    recordRequestSize(bytes, attributes) {
        this.requestSize.record(bytes, attributes);
    }
    recordResponseSize(bytes, attributes) {
        this.responseSize.record(bytes, attributes);
    }
}
const getHttpMetrics = createLazyMetricBundle(() => new HttpMetrics());
export { getHttpMetrics };
//# sourceMappingURL=metrics.js.map