import { context, propagation, } from '@opentelemetry/api';
import { currentContext, updateContext, } from '../context/index.js';
import { RabbitMqHeaders as HeaderNames, } from './constants.js';
const setter = {
    set(carrier, key, value) {
        carrier[key] = value;
    },
};
const getter = {
    keys(carrier) {
        return Object.keys(carrier);
    },
    get(carrier, key) {
        return carrier[key];
    },
};
export function injectContext(properties) {
    properties.headers ??= {};
    const telemetryHeaders = {};
    propagation.inject(context.active(), telemetryHeaders, setter);
    Object.assign(properties.headers, telemetryHeaders);
    const sdkContext = currentContext();
    if (sdkContext.requestId) {
        properties.headers[HeaderNames.REQUEST_ID] = sdkContext.requestId;
    }
    if (sdkContext.correlationId) {
        properties.headers[HeaderNames.CORRELATION_ID] = sdkContext.correlationId;
    }
    if (sdkContext.tenantId) {
        properties.headers[HeaderNames.TENANT_ID] = sdkContext.tenantId;
    }
    if (sdkContext.clientId) {
        properties.headers[HeaderNames.CLIENT_ID] = sdkContext.clientId;
    }
    return properties;
}
export function extractContext(properties) {
    const headers = properties.headers ?? {};
    const telemetryHeaders = {};
    for (const [key, value] of Object.entries(headers)) {
        if (typeof value === 'string') {
            telemetryHeaders[key] = value;
        }
    }
    const update = {};
    const requestId = headers[HeaderNames.REQUEST_ID];
    if (typeof requestId === 'string') {
        update.requestId = requestId;
    }
    const correlationId = headers[HeaderNames.CORRELATION_ID];
    if (typeof correlationId ===
        'string') {
        update.correlationId =
            correlationId;
    }
    const tenantId = headers[HeaderNames.TENANT_ID];
    if (typeof tenantId === 'string') {
        update.tenantId = tenantId;
    }
    const clientId = headers[HeaderNames.CLIENT_ID];
    if (typeof clientId === 'string') {
        update.clientId = clientId;
    }
    updateContext(update);
    return propagation.extract(context.active(), telemetryHeaders, getter);
}
//# sourceMappingURL=context.js.map